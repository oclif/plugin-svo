import * as svo from '@oclif/plugin-svo';
export declare class User {
    id: string;
    name: string;
    constructor(opts: {
        id: string;
        name: string;
    });
}
declare const _default: svo.Subject;
export default _default;
